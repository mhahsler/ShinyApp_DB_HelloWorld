# Add the database credentials here

options(database_server = "servername.smu.edu")
options(database_name = "mydatabase")
options(database_userid = "myuserid")
options(database_password = "mypassword")
