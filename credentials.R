# Add the database credentials here.

options(database_server = "classroomdb.smu.edu")
options(database_port = 55433)
options(database_name = "ShinyTest")
options(database_userid = "shinyapp")
options(database_password = "N0tSecured43887")

